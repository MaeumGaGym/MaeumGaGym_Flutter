import 'package:flutter/material.dart';

class SelfCarePurposeDatePickerDialog extends StatelessWidget {
  const SelfCarePurposeDatePickerDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Dialog(
      child: Container(
        width: 390,
        height: 370,
        child: Column(
          children: [

          ],
        ),
      ),
    );
  }
}
