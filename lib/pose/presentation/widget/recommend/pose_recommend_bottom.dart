import 'package:flutter/material.dart';

class PoseRecommendBottom extends StatelessWidget {
  const PoseRecommendBottom({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
